---
description: 完成本仓库文档/代码改动后自动 git 提交（无需用户再说「提交」）
alwaysApply: true
---

# 自动提交（rs study）

用户已要求：**完成实质性改动后自动 commit**，不必再等用户说「提交」。

## 何时提交

- 完成笔记整理、目录重构、demo 修改、README/01-ER/Book 更新等**有明确交付**的任务后，在回复用户**之前**执行提交。
- 若工作区无变更（或仅 `scratch_test.exe` 等应忽略文件），跳过提交并简要说明。

## 安全边界（仍须遵守）

- **不要**提交：`.env`、密钥、`scratch_test.exe`、`target/` 等构建产物。
- **不要** `git push`，除非用户明确要求。
- **不要**改 `git config`；**不要** `--no-verify`、force push、`commit --amend`（除非用户规则中的 amend 条件全部满足）。
- 提交信息用完整句子，风格与近期 commit 一致（如 `docs(ER):`、`refactor:`、`chore:`）。

## 流程

1. `git status` + `git diff`（有未提交变更时）
2. `git add` 相关文件（不用 `git add -A` 若会带上应忽略项）
3. `git commit -m "..."`（PowerShell 可用 here-string）
4. 回复中简短注明 commit hash 与摘要；**不要**因已自动提交而省略任务说明。
