# 13.1.1 闭包四段式语法演变

← [13.1 闭包](./13.1-闭包.md) · demo：`13.1-closures-demo/`（`quick` 模式含语法示例）

> 官方 The Rust Book 原版写法：从全标注到最简，以及省略规则。

---

## 四段式对照（`add` 为例）

```rust
fn add(x: i32) -> i32 { x + 1 }           // 普通 fn 对照

let add = |x: i32| -> i32 { x + 1 };      // 1. 完整：参数 + 返回 + {}
let add = |x: i32| { x + 1 };             // 2. 省略 ->，保留参数类型
let add = |x: i32| x + 1;                  // 3. 单表达式，省略 {}
let add = |x| x + 1;                      // 4. 最简：全靠推断

add(5); // 6
```

---

## 一、省略规则（核心考点）

### 1. 参数类型可省

`|x|` 不写 `x: i32` — **第一次调用实参确定类型后永久固定**：

```rust
let add = |x| x + 1;
add(5i32);   // x 锁定 i32
// add(5u64); // ❌ 编译报错
```

### 2. 返回值 `-> T` 可省

| 形式 | 规则 |
|------|------|
| **单表达式** `\|x\| x + 1` | 不用写 `->`，返回自动推导 |
| **多语句 `{}`** | 须显式 `-> T`，否则编译报错 |

```rust
let add = |x: i32| -> i32 {
    let tmp = x + 2;
    tmp - 1
};
```

### 3. 单表达式可省 `{}`

- **一条表达式**：`|x| x + 1`
- **多条语句 / 带分号语句**：必须 `{}`

---

## 二、闭包 vs `fn`

| | **`fn`** | **闭包** |
|--|----------|----------|
| 类型标注 | 签名须全标注 | 可推断 |
| 捕获环境 | ❌ | ✅ |

```rust
let base = 10;
let add = |x| base + x; // 捕获外层 base，fn 做不到
println!("{}", add(5)); // 15
```

---

## 三、易错点

**只定义不调用** — 编译器无法推导类型：

```rust
// let add = |x| x + 1; // ❌ 未调用，类型未知
let add = |x| x + 1;
add(5i32); // ✅ 至少调用一次（或写全类型标注）
```

或写全标注：`let add = |x: i32| -> i32 { x + 1 };`

---

## 四、`move` 捕获所有权

→ 详见 [13.1 §四 move 闭包](./13.1-闭包.md#四move-闭包)

```rust
move |x| { /* 强制把捕获变量 move 进闭包 */ }
```

跨线程、FnOnce 场景常用 — demo `thread_move_example`。

---

## demo

```bash
cd Book/13-iterators-closures/13.1-closures-demo
cargo run -- quick   # 含 syntax_examples
```
