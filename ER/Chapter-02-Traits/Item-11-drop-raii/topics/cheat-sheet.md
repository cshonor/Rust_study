# Item 11 · 记忆卡片

← [Item 11 目录](../README.md)

| 要点 | 一句 |
|------|------|
| RAII | 构造拿、析构放；实例在才可用资源 |
| `Drop` | 编译器自动调；别手写 `x.drop()` |
| 提前结束 | `std::mem::drop(x)` |
| 失败释放 | 用 `release()`；Drop 兜底 |
| 锁 | 小作用域 `{}` 尽早解锁 |
